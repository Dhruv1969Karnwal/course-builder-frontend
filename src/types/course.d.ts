export type Course = {
    _id: number;
    title: string;
    description?: string; // Optional
    imageUrl?: string; // Optional
    price?: number; // Optional
    categoryId?: number; // Optional
  };
  